image: java:latest

stages:
  - build
  - deploy

build:
  stage: build
  script: 
    - javac src/org/sindercube/serpex/* -d .
  artifacts:
    paths:
     - Application.class
     - Gui.class
     - PackExtractor.class
     - Presenter.class

deploy:
  stage: deploy
  script:
    - jar cvfe serpex.jar Application *.class
  artifacts:
    paths:
      - serpex.jar
    expire_in: never
